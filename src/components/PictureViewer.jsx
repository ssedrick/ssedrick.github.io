var React = require('react');

var PictureViewer = React.createClass({
   render: function() {
      return (
         <div>
            <h2>Header!</h2>
         </div>
      );
   }
});

module.exports = PictureViewer;
